name: "01 - Building Blocks"

on:
  push:
    branches:
        - main
  workflow_dispatch:

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: "Say Hello"
              run: echo "Hello, World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: "First step"
              run: |
                echo "I should succseed"
                exit 0
            - name: "Say Goodbye"
              run: echo "Goodbye!"